// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct LDAPResult {
    pub result_code: LDAPResultResultCode,
    pub matched_dn: LDAPDN,
    pub diagnostic_message: LDAPString,
    pub referral: Option<LDAPReferral>,
}

impl LDAPResult {
    pub fn new(result_code: LDAPResultResultCode, matched_dn: LDAPDN, diagnostic_message: LDAPString, referral: Option<LDAPReferral>) -> Self {
        Self {
            result_code,
            matched_dn,
            diagnostic_message,
            referral,
        }
    }
}

impl DERParseable for LDAPResult {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let result_code = LDAPResultResultCode::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let matched_dn = LDAPDN::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let diagnostic_message = LDAPString::from_der_node(nodes.next().ok_or(ASN1Error::new(rust_asn1::errors::ErrorCode::TruncatedASN1Field, "Premature end of data".to_string(), file!().to_string(), line!()))?)?;
            let referral: Option<LDAPReferral> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match LDAPReferral::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                result_code: result_code,
                matched_dn: matched_dn,
                diagnostic_message: diagnostic_message,
                referral: referral,
            })

        })
    }
}

impl DERSerializable for LDAPResult {
    fn serialize(&self, _serializer: &mut Serializer) -> Result<(), ASN1Error> {
        Err(ASN1Error::new(
            rust_asn1::errors::ErrorCode::UnsupportedFieldLength,
            "Serialization is not implemented by the generator yet.".to_string(),
            file!().to_string(),
            line!(),
        ))
    }
}

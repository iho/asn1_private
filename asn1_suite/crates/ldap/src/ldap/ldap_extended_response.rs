// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct LDAPExtendedResponse {
    pub response_name: Option<LDAPOID>,
    pub response_value: Option<ASN1OctetString>,
}

impl LDAPExtendedResponse {
    pub fn new(response_name: Option<LDAPOID>, response_value: Option<ASN1OctetString>) -> Self {
        Self {
            response_name,
            response_value,
        }
    }
}

impl DERParseable for LDAPExtendedResponse {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;

            let response_name: Option<LDAPOID> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match LDAPOID::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let response_value: Option<ASN1OctetString> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1OctetString::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {

                response_name: response_name,
                response_value: response_value,
            })

        })
    }
}

impl DERSerializable for LDAPExtendedResponse {
    fn serialize(&self, _serializer: &mut Serializer) -> Result<(), ASN1Error> {
        Err(ASN1Error::new(
            rust_asn1::errors::ErrorCode::UnsupportedFieldLength,
            "Serialization is not implemented by the generator yet.".to_string(),
            file!().to_string(),
            line!(),
        ))
    }
}

// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct SelectedAttributeTypesPeriod {
    pub times_of_day: Option<Vec<SelectedAttributeTypesDayTimeBand>>,
    pub days: Option<SelectedAttributeTypesPeriodDaysChoice>,
    pub weeks: Option<SelectedAttributeTypesPeriodWeeksChoice>,
    pub months: Option<SelectedAttributeTypesPeriodMonthsChoice>,
    pub years: Option<Vec<ASN1Integer>>,
}

impl SelectedAttributeTypesPeriod {
    pub fn new(times_of_day: Option<Vec<SelectedAttributeTypesDayTimeBand>>, days: Option<SelectedAttributeTypesPeriodDaysChoice>, weeks: Option<SelectedAttributeTypesPeriodWeeksChoice>, months: Option<SelectedAttributeTypesPeriodMonthsChoice>, years: Option<Vec<ASN1Integer>>) -> Self {
        Self {
            times_of_day,
            days,
            weeks,
            months,
            years,
        }
    }
}

impl DERParseable for SelectedAttributeTypesPeriod {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let times_of_day: Option<Vec<SelectedAttributeTypesDayTimeBand>> = if let Some(node) = nodes.peek().map(|n| n.clone()) { if let rust_asn1::asn1::Content::Constructed(collection) = &node.content { nodes.next(); match collection.clone().into_iter().map(|child| SelectedAttributeTypesDayTimeBand::from_der_node(child)).collect::<Result<_, _>>() { Ok(v) => Some(v), Err(_) => None } } else { None } } else { None };
            let days: Option<SelectedAttributeTypesPeriodDaysChoice> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match SelectedAttributeTypesPeriodDaysChoice::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let weeks: Option<SelectedAttributeTypesPeriodWeeksChoice> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match SelectedAttributeTypesPeriodWeeksChoice::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let months: Option<SelectedAttributeTypesPeriodMonthsChoice> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match SelectedAttributeTypesPeriodMonthsChoice::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let years: Option<Vec<ASN1Integer>> = if let Some(node) = nodes.peek().map(|n| n.clone()) { if let rust_asn1::asn1::Content::Constructed(collection) = &node.content { nodes.next(); match collection.clone().into_iter().map(|child| ASN1Integer::from_der_node(child)).collect::<Result<_, _>>() { Ok(v) => Some(v), Err(_) => None } } else { None } } else { None };
            Ok(Self {
                times_of_day: times_of_day,
                days: days,
                weeks: weeks,
                months: months,
                years: years,
            })

        })
    }
}

impl DERSerializable for SelectedAttributeTypesPeriod {
    fn serialize(&self, _serializer: &mut Serializer) -> Result<(), ASN1Error> {
        Err(ASN1Error::new(
            rust_asn1::errors::ErrorCode::UnsupportedFieldLength,
            "Serialization is not implemented by the generator yet.".to_string(),
            file!().to_string(),
            line!(),
        ))
    }
}

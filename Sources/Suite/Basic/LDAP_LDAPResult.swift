// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline struct LDAP_LDAPResult: Equatable, Hashable, Codable, Sendable {
    @usableFromInline var resultCode: LDAP_LDAPResult_resultCode_Enum
    @usableFromInline var matchedDN: LDAP_LDAPDN
    @usableFromInline var diagnosticMessage: LDAP_LDAPString
    @usableFromInline var referral: LDAP_Referral?
    @inlinable init(resultCode: LDAP_LDAPResult_resultCode_Enum, matchedDN: LDAP_LDAPDN, diagnosticMessage: LDAP_LDAPString, referral: LDAP_Referral?) {
        self.resultCode = resultCode
        self.matchedDN = matchedDN
        self.diagnosticMessage = diagnosticMessage
        self.referral = referral
    }

    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            self.resultCode = try DER.decode(LDAP_LDAPResult_resultCode_Enum.self)
            self.matchedDN = try DER.decode(LDAP_LDAPDN.self)
            self.diagnosticMessage = try DER.decode(LDAP_LDAPString.self)
            self.referral = try DER.decode(LDAP_Referral.self)
    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            try coder.serialize(resultCode)
            try coder.serialize(matchedDN)
            try coder.serialize(diagnosticMessage)
            if let referral = self.referral { if let referral = self.referral { try coder.serialize(referral) } }
    }

}

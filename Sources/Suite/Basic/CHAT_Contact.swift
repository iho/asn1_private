// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline struct CHAT_Contact: Equatable, Hashable, Codable, Sendable {
    @usableFromInline var nickname: ASN1OctetString
    @usableFromInline var avatar: ASN1OctetString
    @usableFromInline var names: [ASN1OctetString]
    @usableFromInline var phone-id: ASN1OctetString
    @usableFromInline var surnames: [ASN1OctetString]
    @usableFromInline var last-msg: CHAT_Message
    @usableFromInline var presence: CHAT_PresenceType
    @usableFromInline var update: Int
    @usableFromInline var created: Int
    @usableFromInline var settings: [CHAT_Feature]
    @usableFromInline var services: [CHAT_Service]
    @usableFromInline var status: CHAT_ContactStatus
    @inlinable init(nickname: ASN1OctetString, avatar: ASN1OctetString, names: [ASN1OctetString], phone-id: ASN1OctetString, surnames: [ASN1OctetString], last-msg: CHAT_Message, presence: CHAT_PresenceType, update: Int, created: Int, settings: [CHAT_Feature], services: [CHAT_Service], status: CHAT_ContactStatus) {
        self.nickname = nickname
        self.avatar = avatar
        self.names = names
        self.phone-id = phone-id
        self.surnames = surnames
        self.last-msg = last-msg
        self.presence = presence
        self.update = update
        self.created = created
        self.settings = settings
        self.services = services
        self.status = status
    }

    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            self.nickname = try DER.decode(ASN1OctetString.self)
            self.avatar = try DER.decode(ASN1OctetString.self)
            self.names = try DER.sequence(of: ASN1OctetString.self, identifier: .sequence, rootNode: rootNode)
            self.phone-id = try DER.decode(ASN1OctetString.self)
            self.surnames = try DER.sequence(of: ASN1OctetString.self, identifier: .sequence, rootNode: rootNode)
            self.last-msg = try DER.decode(CHAT_Message.self)
            self.presence = try DER.decode(CHAT_PresenceType.self)
            self.update = try DER.decode(Int.self)
            self.created = try DER.decode(Int.self)
            self.settings = try DER.sequence(of: CHAT_Feature.self, identifier: .sequence, rootNode: rootNode)
            self.services = try DER.sequence(of: CHAT_Service.self, identifier: .sequence, rootNode: rootNode)
            self.status = try DER.decode(CHAT_ContactStatus.self)
    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            try coder.serialize(nickname)
            try coder.serialize(avatar)
            try coder.serialize(names)
            try coder.serialize(phone-id)
            try coder.serialize(surnames)
            try coder.serialize(last-msg)
            try coder.serialize(presence)
            try coder.serialize(update)
            try coder.serialize(created)
            try coder.serialize(settings)
            try coder.serialize(services)
            try coder.serialize(status)
    }

}

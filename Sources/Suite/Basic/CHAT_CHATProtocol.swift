// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline enum CHAT_CHATProtocol: Equatable, Hashable, Codable, Sendable {
    case register(CHAT_Register)
    case auth(CHAT_Auth)
    case feature(CHAT_Feature)
    case service(CHAT_Service)
    case message(CHAT_Message)
    case profile(CHAT_Profile)
    case room(CHAT_Room)
    case member(CHAT_Member)
    case search(CHAT_Search)
    case file(CHAT_FileDesc)
    case typing(CHAT_Typing)
    case friend(CHAT_Friend)
    case presence(CHAT_Presence)
    case history(CHAT_History)
    case roster(CHAT_Roster)
    @inlinable static var defaultIdentifier: ASN1Identifier { .enumerated }
    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            if let v = try? ASN1Any.decode(CHAT_Register.self, from: rootNode) { self = .register(v); return }
            if let v = try? ASN1Any.decode(CHAT_Auth.self, from: rootNode) { self = .auth(v); return }
            if let v = try? ASN1Any.decode(CHAT_Feature.self, from: rootNode) { self = .feature(v); return }
            if let v = try? ASN1Any.decode(CHAT_Service.self, from: rootNode) { self = .service(v); return }
            if let v = try? ASN1Any.decode(CHAT_Message.self, from: rootNode) { self = .message(v); return }
            if let v = try? ASN1Any.decode(CHAT_Profile.self, from: rootNode) { self = .profile(v); return }
            if let v = try? ASN1Any.decode(CHAT_Room.self, from: rootNode) { self = .room(v); return }
            if let v = try? ASN1Any.decode(CHAT_Member.self, from: rootNode) { self = .member(v); return }
            if let v = try? ASN1Any.decode(CHAT_Search.self, from: rootNode) { self = .search(v); return }
            if let v = try? ASN1Any.decode(CHAT_FileDesc.self, from: rootNode) { self = .file(v); return }
            if let v = try? ASN1Any.decode(CHAT_Typing.self, from: rootNode) { self = .typing(v); return }
            if let v = try? ASN1Any.decode(CHAT_Friend.self, from: rootNode) { self = .friend(v); return }
            if let v = try? ASN1Any.decode(CHAT_Presence.self, from: rootNode) { self = .presence(v); return }
            if let v = try? ASN1Any.decode(CHAT_History.self, from: rootNode) { self = .history(v); return }
            if let v = try? ASN1Any.decode(CHAT_Roster.self, from: rootNode) { self = .roster(v); return }
    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            case .register(let val): try coder.serialize(val)
            case .auth(let val): try coder.serialize(val)
            case .feature(let val): try coder.serialize(val)
            case .service(let val): try coder.serialize(val)
            case .message(let val): try coder.serialize(val)
            case .profile(let val): try coder.serialize(val)
            case .room(let val): try coder.serialize(val)
            case .member(let val): try coder.serialize(val)
            case .search(let val): try coder.serialize(val)
            case .file(let val): try coder.serialize(val)
            case .typing(let val): try coder.serialize(val)
            case .friend(let val): try coder.serialize(val)
            case .presence(let val): try coder.serialize(val)
            case .history(let val): try coder.serialize(val)
            case .roster(let val): try coder.serialize(val)
    }

}

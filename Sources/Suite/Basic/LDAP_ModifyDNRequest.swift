// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline struct LDAP_ModifyDNRequest: Equatable, Hashable, Codable, Sendable {
    @usableFromInline var entry: LDAP_LDAPDN
    @usableFromInline var newrdn: LDAP_RelativeLDAPDN
    @usableFromInline var deleteoldrdn: Bool
    @usableFromInline var newSuperior: LDAP_LDAPDN?
    @inlinable init(entry: LDAP_LDAPDN, newrdn: LDAP_RelativeLDAPDN, deleteoldrdn: Bool, newSuperior: LDAP_LDAPDN?) {
        self.entry = entry
        self.newrdn = newrdn
        self.deleteoldrdn = deleteoldrdn
        self.newSuperior = newSuperior
    }

    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            self.entry = try DER.decode(LDAP_LDAPDN.self)
            self.newrdn = try DER.decode(LDAP_RelativeLDAPDN.self)
            self.deleteoldrdn = try DER.decode(Bool.self)
            self.newSuperior = try DER.decode(LDAP_LDAPDN.self)
    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            try coder.serialize(entry)
            try coder.serialize(newrdn)
            try coder.serialize(deleteoldrdn)
            if let newSuperior = self.newSuperior { if let newSuperior = self.newSuperior { try coder.serialize(newSuperior) } }
    }

}

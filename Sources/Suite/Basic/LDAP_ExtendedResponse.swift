// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline struct LDAP_ExtendedResponse: Equatable, Hashable, Codable, Sendable {
    @usableFromInline var responseName: LDAP_LDAPOID?
    @usableFromInline var responseValue: ASN1OctetString?
    @inlinable init(resultCode: LDAPResult_resultCode_Enum, matchedDN: LDAP_LDAPDN, diagnosticMessage: LDAP_LDAPString, referral: LDAP_Referral?, responseName: LDAP_LDAPOID?, responseValue: ASN1OctetString?) {
        self.resultCode = resultCode
        self.matchedDN = matchedDN
        self.diagnosticMessage = diagnosticMessage
        self.referral = referral
        self.responseName = responseName
        self.responseValue = responseValue
    }

    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            self.resultCode = try DER.decode(LDAPResult_resultCode_Enum.self)
            self.matchedDN = try DER.decode(LDAP_LDAPDN.self)
            self.diagnosticMessage = try DER.decode(LDAP_LDAPString.self)
            self.referral = try DER.decode(LDAP_Referral.self)
            self.responseName = try DER.decode(LDAP_LDAPOID.self)
            self.responseValue = try DER.decode(ASN1OctetString.self)
    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            try coder.serialize(resultCode)
            try coder.serialize(matchedDN)
            try coder.serialize(diagnosticMessage)
            if let referral = self.referral { if let referral = self.referral { try coder.serialize(referral) } }
            if let responseName = self.responseName { if let responseName = self.responseName { try coder.serialize(responseName) } }
            if let responseValue = self.responseValue { if let responseValue = self.responseValue { try coder.serialize(responseValue) } }
    }

}

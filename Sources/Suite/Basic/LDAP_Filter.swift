// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline enum LDAP_Filter: Equatable, Hashable, Codable, Sendable {
    case and([LDAP_Filter])
    case or([LDAP_Filter])
    case not(LDAP_Filter)
    case equalityMatch(LDAP_AttributeValueAssertion)
    case substrings(LDAP_SubstringFilter)
    case greaterOrEqual(LDAP_AttributeValueAssertion)
    case lessOrEqual(LDAP_AttributeValueAssertion)
    case present(LDAP_AttributeDescription)
    case approxMatch(LDAP_AttributeValueAssertion)
    case extensibleMatch(LDAP_MatchingRuleAssertion)

    @inlinable static var defaultIdentifier: ASN1Identifier { .enumerated }
    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            if let v = try? DER.set(of: LDAP_Filter.self, from: rootNode) { self = .and(v); return }
            if let v = try? DER.set(of: LDAP_Filter.self, from: rootNode) { self = .or(v); return }
            if let v = try? ASN1Any.decode(LDAP_Filter.self, from: rootNode) { self = .not(v); return }
            if let v = try? ASN1Any.decode(LDAP_AttributeValueAssertion.self, from: rootNode) { self = .equalityMatch(v); return }
            if let v = try? ASN1Any.decode(LDAP_SubstringFilter.self, from: rootNode) { self = .substrings(v); return }
            if let v = try? ASN1Any.decode(LDAP_AttributeValueAssertion.self, from: rootNode) { self = .greaterOrEqual(v); return }
            if let v = try? ASN1Any.decode(LDAP_AttributeValueAssertion.self, from: rootNode) { self = .lessOrEqual(v); return }
            if let v = try? ASN1Any.decode(LDAP_AttributeDescription.self, from: rootNode) { self = .present(v); return }
            if let v = try? ASN1Any.decode(LDAP_AttributeValueAssertion.self, from: rootNode) { self = .approxMatch(v); return }
            if let v = try? ASN1Any.decode(LDAP_MatchingRuleAssertion.self, from: rootNode) { self = .extensibleMatch(v); return }

    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            case .and(let val): try coder.serialize(val)
            case .or(let val): try coder.serialize(val)
            case .not(let val): try coder.serialize(val)
            case .equalityMatch(let val): try coder.serialize(val)
            case .substrings(let val): try coder.serialize(val)
            case .greaterOrEqual(let val): try coder.serialize(val)
            case .lessOrEqual(let val): try coder.serialize(val)
            case .present(let val): try coder.serialize(val)
            case .approxMatch(let val): try coder.serialize(val)
            case .extensibleMatch(let val): try coder.serialize(val)

    }

}

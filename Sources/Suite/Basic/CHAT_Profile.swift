// Generated by ASN1.ERP.UNO Compiler, Copyright © 2023—2024 Namdak Tönpa.
import SwiftASN1
import Foundation

@usableFromInline struct CHAT_Profile: Equatable, Hashable, Codable, Sendable {
    @usableFromInline var nickname: ASN1OctetString
    @usableFromInline var phone: ASN1OctetString
    @usableFromInline var session: ASN1OctetString
    @usableFromInline var chats: [CHAT_Contact]
    @usableFromInline var contacts: [CHAT_Contact]
    @usableFromInline var keys: [ASN1OctetString]
    @usableFromInline var servers: [CHAT_Server]
    @usableFromInline var settings: [CHAT_Feature]
    @usableFromInline var update: Int
    @usableFromInline var status: Int
    @usableFromInline var roster: CHAT_Roster
    @inlinable init(nickname: ASN1OctetString, phone: ASN1OctetString, session: ASN1OctetString, chats: [CHAT_Contact], contacts: [CHAT_Contact], keys: [ASN1OctetString], servers: [CHAT_Server], settings: [CHAT_Feature], update: Int, status: Int, roster: CHAT_Roster) {
        self.nickname = nickname
        self.phone = phone
        self.session = session
        self.chats = chats
        self.contacts = contacts
        self.keys = keys
        self.servers = servers
        self.settings = settings
        self.update = update
        self.status = status
        self.roster = roster
    }

    @inlinable public init(derEncoded rootNode: ASN1Node, withIdentifier identifier: ASN1Identifier) throws {
            self.nickname = try DER.decode(ASN1OctetString.self)
            self.phone = try DER.decode(ASN1OctetString.self)
            self.session = try DER.decode(ASN1OctetString.self)
            self.chats = try DER.sequence(of: CHAT_Contact.self, identifier: .sequence, rootNode: rootNode)
            self.contacts = try DER.sequence(of: CHAT_Contact.self, identifier: .sequence, rootNode: rootNode)
            self.keys = try DER.sequence(of: ASN1OctetString.self, identifier: .sequence, rootNode: rootNode)
            self.servers = try DER.sequence(of: CHAT_Server.self, identifier: .sequence, rootNode: rootNode)
            self.settings = try DER.sequence(of: CHAT_Feature.self, identifier: .sequence, rootNode: rootNode)
            self.update = try DER.decode(Int.self)
            self.status = try DER.decode(Int.self)
            self.roster = try DER.decode(CHAT_Roster.self)
    }

    @inlinable func serialize(into coder: inout DER.Serializer, withIdentifier identifier: ASN1Identifier) throws {
            try coder.serialize(nickname)
            try coder.serialize(phone)
            try coder.serialize(session)
            try coder.serialize(chats)
            try coder.serialize(contacts)
            try coder.serialize(keys)
            try coder.serialize(servers)
            try coder.serialize(settings)
            try coder.serialize(update)
            try coder.serialize(status)
            try coder.serialize(roster)
    }

}

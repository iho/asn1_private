// Generated by ASN1.ERP.UNO Compiler -- Rust emitter

use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub struct PKIX1Explicit88PDSParameter {
    pub printablestring: Option<ASN1PrintableString>,
    pub teletexstring: Option<ASN1UTF8String>,
}

impl PKIX1Explicit88PDSParameter {
    pub fn new(printablestring: Option<ASN1PrintableString>, teletexstring: Option<ASN1UTF8String>) -> Self {
        Self {
            printablestring,
            teletexstring,
        }
    }
}

impl DERParseable for PKIX1Explicit88PDSParameter {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        der::sequence(node, ASN1Identifier::SEQUENCE, |nodes| {
            let all_nodes: Vec<ASN1Node> = nodes.collect();
            let mut iter = all_nodes.into_iter().peekable();
            let nodes = &mut iter;
            let printablestring: Option<ASN1PrintableString> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1PrintableString::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            let teletexstring: Option<ASN1UTF8String> = if let Some(node) = nodes.peek().map(|n| n.clone()) { match ASN1UTF8String::from_der_node(node.clone()) { Ok(val) => { nodes.next(); Some(val) }, Err(_) => None } } else { None };
            Ok(Self {
                printablestring: printablestring,
                teletexstring: teletexstring,
            })

        })
    }
}

impl DERSerializable for PKIX1Explicit88PDSParameter {
    fn serialize(&self, _serializer: &mut Serializer) -> Result<(), ASN1Error> {
        Err(ASN1Error::new(
            rust_asn1::errors::ErrorCode::UnsupportedFieldLength,
            "Serialization is not implemented by the generator yet.".to_string(),
            file!().to_string(),
            line!(),
        ))
    }
}

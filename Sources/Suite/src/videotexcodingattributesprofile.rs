  // Generated by ASN1.ERP.UNO Compiler -- Rust emitter
use std::sync::Arc;
use rust_asn1::{
    asn1::ASN1Node,
    asn1_types::*,
    der::{self, DERParseable, DERSerializable, Serializer},
    errors::ASN1Error,
};
use super::*;

#[derive(Clone, Debug)]
pub enum VideotexCodingAttributesProfile {
    Undefined = 0,
    Profile1 = 81,
    Profile2 = 82,
    Profile3 = 83,
    Profile4 = 84,
    ProfileX11 = 85,
    ProfileX12 = 86,
    ProfileX13 = 87,
    ProfileX14 = 88,
    ProfileX21 = 89,
    ProfileX22 = 90,
    ProfileX23 = 91,
    ProfileX24 = 92,
}

impl DERParseable for VideotexCodingAttributesProfile {
    fn from_der_node(node: ASN1Node) -> Result<Self, ASN1Error> {
        let integer = ASN1Integer::from_der_node(node)?;
        // Attempt to convert BigInt to i64
        // ASN1Integer usually wraps BigInt in `value`.
        // We need to import TryInto or use a conversion method.
        // Using `TryInto` requires `use std::convert::TryInto;` in scope or prelude.
        // If `ASN1Integer` doesn't impl `TryInto<i64>`, maybe `integer.value` does.
        // Assuming `integer.value` is BigInt and `num_traits` or similar is available via `rust-asn1`.
        // If not available, we can try formatting to string and parsing (slow but robust fallback?).
        // Or assume `try_into` works on `value`.
        // Error said: `required for ASN1Integer to implement TryInto`. So `integer.try_into()` failed.
        // Let's try `integer.value.try_into()`.
        let val_res: Result<i64, _> = integer.value.try_into();
        let val = val_res.map_err(|_| ASN1Error::new(rust_asn1::errors::ErrorCode::InvalidASN1Object, "Enum value out of range".to_string(), file!().to_string(), line!()))?;
        match val {
            0 => Ok(VideotexCodingAttributesProfile::Undefined),
            81 => Ok(VideotexCodingAttributesProfile::Profile1),
            82 => Ok(VideotexCodingAttributesProfile::Profile2),
            83 => Ok(VideotexCodingAttributesProfile::Profile3),
            84 => Ok(VideotexCodingAttributesProfile::Profile4),
            85 => Ok(VideotexCodingAttributesProfile::ProfileX11),
            86 => Ok(VideotexCodingAttributesProfile::ProfileX12),
            87 => Ok(VideotexCodingAttributesProfile::ProfileX13),
            88 => Ok(VideotexCodingAttributesProfile::ProfileX14),
            89 => Ok(VideotexCodingAttributesProfile::ProfileX21),
            90 => Ok(VideotexCodingAttributesProfile::ProfileX22),
            91 => Ok(VideotexCodingAttributesProfile::ProfileX23),
            92 => Ok(VideotexCodingAttributesProfile::ProfileX24),
            _ => Err(ASN1Error::new(rust_asn1::errors::ErrorCode::InvalidASN1Object, format!("Unknown value for VideotexCodingAttributesProfile: {}", val), file!().to_string(), line!()))
        }
    }
}

impl DERSerializable for VideotexCodingAttributesProfile {
    fn serialize(&self, _serializer: &mut Serializer) -> Result<(), ASN1Error> {
        Err(ASN1Error::new(
            rust_asn1::errors::ErrorCode::UnsupportedFieldLength,
            "Serialization is not implemented by the generator yet.".to_string(),
            file!().to_string(),
            line!(),
        ))
    }
}

